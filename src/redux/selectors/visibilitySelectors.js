export const selectVisibleAirlines = (state) => state.VisibleState.airlines

export const selectVisibleStops = (state) => state.VisibleState.stops

export const selectVisibleDepartures = (state) => state.VisibleState.departures

export const selectVisiblePrices = (state) => state.VisibleState.prices
